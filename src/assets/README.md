# Source assets

Assets imported by React and CSS live here so Vite can fingerprint them during
the production build. Large content media referenced by section markup remains
in `public/assets`, while videos live in `public/media/videos`.
