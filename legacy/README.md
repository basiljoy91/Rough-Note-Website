# Legacy archive

These files powered the pre-refactor static site and its three separately
generated React bundles. They are not imported by the current application.

They remain here temporarily so the directory migration is recoverable. Once
the Vite multi-page version has been accepted in production, this archive can
be removed in a dedicated cleanup commit.
